[workspace]
members = [
    "migration-test-contract",
    "owner-contract",
    "tests",
]
default-members = [
    "migration-test-contract",
    "owner-contract",
    "tests",
]
resolver = "2"


[profile.release]
codegen-units = 1
lto = true